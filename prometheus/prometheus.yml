global:
  scrape_interval: 5s
  evaluation_interval: 5s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
          - 'alertmanager:9093'

rule_files:
  - "alert.yml"

scrape_configs:
  - job_name: 'nodejs-app'
    static_configs:
      - targets: ['app:5000']